services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    working_dir: /app
    command: npm run dev
    environment:
      - NODE_ENV=development
    ports:
      - "4444:4444"
    tty: true
